// jsts (JavaScript Topology Suite) n'expose pas de types officiels.
// On le déclare en `any` : utilisé uniquement dans `polygonize.ts` pour le
// noding (UnaryUnionOp) et la reconstruction de polygones (Polygonizer).
declare module "jsts";
